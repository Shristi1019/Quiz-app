# Quiz-app
### To run this project, follow the steps given below - 
First, clone this repository.
Move to directory named API and run the command npm i to download node modules.
To run the Backend server, execute the command node server.js.
Move to directory named Frontend and run the command npm i to download node modules.
To run the Frontend, execute the command npm start.
